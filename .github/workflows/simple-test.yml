name: Setting our first workflow

on: [push]

jobs: 
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
    - name: echo a string
      run: echo "My name is amardeep"
    - name: multiline echo
      run: |
        npm -v
        node -v